diff --git a/lib/interface.js b/lib/interface.js
index fd7b14e..4575281 100644
--- a/lib/interface.js
+++ b/lib/interface.js
@@ -35,6 +35,13 @@ function defaultOptions(options) {
   return options;
 }
 
+function VariableContext(varName) {
+  this.name = varName
+  this.contexts = []
+}
+
+VariableContext.EMPTY = new VariableContext()
+
 module.exports = function(context, state, options) {
   options = defaultOptions(options || {});
 
@@ -51,19 +58,24 @@ module.exports = function(context, state, options) {
   };
 
   context.get = function(varName) {
-    var originalSuite = state.currentTestContext;
+    var context = state.currentTestContext
+    var variable = context[currentlyRetrievedVarField] || VariableContext.EMPTY
+
+    if (varName === variable.name) {
+      var lastContext = variable.contexts[variable.contexts.length - 1] || context
+      var parentContext = getParentContextFor(varName, lastContext);
+
+      variable.contexts.push(parentContext)
 
-    if (varName === state.currentTestContext[currentlyRetrievedVarField]) {
-      state.currentTestContext = getParentContextFor(varName, originalSuite);
+      return parentContext[varName]
     }
 
     try {
-      state.currentTestContext[currentlyRetrievedVarField] = varName;
+      context[currentlyRetrievedVarField] = new VariableContext(varName)
 
-      return state.currentTestContext[varName];
+      return context[varName];
     } finally {
-      delete state.currentTestContext[currentlyRetrievedVarField];
-      state.currentTestContext = originalSuite;
+      delete context[currentlyRetrievedVarField];
     }
   };
 
